## 1. 修复 imageUseCase.ts 中的 calculateImageFit 函数

- [x] 1.1 修改 `src/useCases/imageUseCase.ts` 中的 `calculateImageFit` 函数，计算正确的居中偏移量
- [x] 1.2 验证修复后的函数逻辑正确

## 2. 修复 useTemplateApplication.ts 中的 calculateImageFit 函数

- [x] 2.1 修改 `src/hooks/useTemplateApplication.ts` 中的 `calculateImageFit` 函数，计算正确的居中偏移量
- [x] 2.2 验证修复后的函数逻辑正确

## 3. 修复画布容器居中问题

- [x] 3.1 在 `src/components/CanvasEditor/CanvasEditor.tsx` 中为 Stage 添加居中容器
- [x] 3.2 运行构建命令验证代码无语法错误
